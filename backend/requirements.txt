# ── Core web framework ────────────────────────────────────────
fastapi==0.115.12
uvicorn[standard]==0.34.2
gunicorn==23.0.0
python-dotenv==1.1.0
python-multipart==0.0.20

# ── Data validation ───────────────────────────────────────────
pydantic[email]==2.10.6

# ── Database ──────────────────────────────────────────────────
sqlalchemy==2.0.40
alembic==1.15.2
psycopg2-binary==2.9.10

# ── System / utilities ────────────────────────────────────────
psutil==7.0.0
requests==2.32.3
httpx==0.28.1

# ── Data science (lightweight) ────────────────────────────────
pandas==2.2.3
numpy==1.26.4
# IMPORTANT: Models trained with 1.6.1 — do NOT upgrade
scikit-learn==1.6.1
joblib==1.4.2

# ── NLP utilities ─────────────────────────────────────────────
langdetect==1.0.9

# ── Auth ──────────────────────────────────────────────────────
python-jose[cryptography]==3.3.0
bcrypt==4.1.3
google-auth==2.40.1
google-auth-oauthlib==1.2.1

# ── AI / LLM clients ─────────────────────────────────────────
google-generativeai==0.8.5
groq==0.25.0
cerebras-cloud-sdk==1.29.0
openai==1.78.0

# ── Caching ───────────────────────────────────────────────────
redis==5.2.1
hiredis==3.1.1

# ── Monitoring ────────────────────────────────────────────────
prometheus-client==0.21.1

# ── PDF / DOCX parsing (/upload endpoint) ────────────────────
pdfplumber==0.11.4
python-docx==1.1.2

# ── Social graph (optional — only used when SOCIAL_GRAPH_ENABLED=true) ───────
praw==7.8.1
tweepy==4.15.0

# ── Transformer models (OPTIONAL — NOT installed on Render free tier) ────────
# Render free tier build times out installing torch (~800MB).
# These are excluded from the default install via pip extras.
# To enable locally: pip install torch transformers sentence-transformers hdbscan
# On Render paid tier: uncomment these lines in requirements.txt
# transformers==4.51.3
# torch==2.6.0
# sentence-transformers==3.4.1
# hdbscan==0.8.40

# ── Testing ───────────────────────────────────────────────────
pytest==8.3.5
